## Summary

<!-- What changed and why -->

## Test plan

- [ ] `npm run lint` passes in `FerrariSite/`
- [ ] `npm run build` passes in `FerrariSite/`
- [ ] No GSAP `markers: true` left in production code
- [ ] UI changes include before/after screenshots or screen recording
- [ ] GLB or asset changes are noted (paths under `public/models/` or `Ferrari/`)
